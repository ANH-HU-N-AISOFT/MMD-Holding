import { FC } from 'react';
import { Notification } from '~/packages/common/InteralError/components/Notification';

export const Page: FC = () => {
  return <Notification />;
};

export default Page;
